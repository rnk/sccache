services:
  # AMQP broker for scheduler and server communication
  rabbitmq:
    hostname: rabbitmq
    image: rabbitmq:management
    ports: ["5672:5672", "15672:15672"]
    volumes: ["./rabbitmq-single.conf:/etc/rabbitmq/rabbitmq.conf:ro"]
